<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopilyze</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: #f4f4f4;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: #333;
            color: white;
            padding: 1rem;
            text-align: center;
        }

        header nav ul {
            list-style-type: none;
            padding: 0;
        }

        header nav ul li {
            display: inline;
            margin: 0 1rem;
        }

        header nav ul li a {
            color: white;
            text-decoration: none;
        }

        main {
            padding: 2rem;
            text-align: center;
            flex: 1;
        }

        #product-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .product {
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin: 1rem;
            padding: 1rem;
            width: 250px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: left;
            cursor: pointer;
        }

        .product img {
            max-width: 100%;
            height: auto;
            border-bottom: 1px solid #ddd;
            margin-bottom: 1rem;
        }

        .product h3 {
            font-size: 1.2rem;
            margin: 0.5rem 0;
        }

        .product p {
            margin: 0.5rem 0;
        }

        footer {
            background-color: #333;
            color: white;
            padding: 1rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        <h1>Shopilyze</h1>
        <nav>
            <ul>
                <li><a href="#home">Главная</a></li>
                <li><a href="#products">Продукты</a></li>
                <li><a href="#contact">Контакты</a></li>
            </ul>
        </nav>
    </header>

    <main id="products">
        <h2>Продукты</h2>
        <div id="product-list"></div>
    </main>

    <footer>
        <p>&copy; 2024 Shopilyze. Все права защищены.</p>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const productList = document.getElementById('product-list');

            fetch('https://marketplace-800v.onrender.com/api/product')
                .then(response => response.json())
                .then(products => {
                    products.forEach(product => {
                        const productElement = document.createElement('div');
                        productElement.classList.add('product');
                        productElement.setAttribute('data-id', product.id);

                        productElement.innerHTML = `
                            <img src="${product.pictureUrl}" alt="${product.title.ru}">
                            <h3>${product.title.ru}</h3>
                            <p>${product.description.ru}</p>
                            <p>Цена: $${product.price}</p>
                        `;

                        productList.appendChild(productElement);

                        // Добавляем обработчик клика для перехода на страницу продукта
                        productElement.addEventListener('click', () => {
                            window.location.href = `/Home/Product?id=${product.id}`;
                        });
                    });
                })
                .catch(error => {
                    console.error('Ошибка при загрузке данных:', error);
                    productList.innerHTML = '<p>Не удалось загрузить продукты.</p>';
                });
        });
    </script>
</body>
</html>